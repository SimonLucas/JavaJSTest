package test

import java.net.URI
import java.net.URL

fun main() {
    val url = URL("http://simon.ai")
    // val uri = URI(scheme = "http://simon.ai", query = "hello=12")
    // uri.
    // url.query = "hello"
    // val builder = Uri.
    println(url)
}
